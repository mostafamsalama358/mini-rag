# __init__.py

## What is this?

Empty package marker for `stores/llm/`.

## Why does it exist?

Allows imports like `from stores.llm.LLMProviderFactory import ...`

## Where is it used?

- `src/main.py`, `src/celery_app.py` import from this package
