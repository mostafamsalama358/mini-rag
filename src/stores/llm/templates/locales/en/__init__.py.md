# __init__.py (locales/en)

## What is this?

Empty package marker for English locale templates.

## Why does it exist?

Python needs this so `template_parser` can import `stores.llm.templates.locales.en.rag`.

## Where is it used?

| File | How |
|------|-----|
| `src/stores/llm/templates/locales/en/rag.py` | English RAG prompts |
