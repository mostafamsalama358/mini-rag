# __init__.py (locales)

## What is this?

Empty package marker for `templates/locales/`.

## Why does it exist?

Holds language folders: `en/` and `ar/`.

## Where is it used?

| File | How |
|------|-----|
| `src/stores/llm/templates/template_parser.py` | Builds path `locales/{language}/rag.py` |
