# __init__.py (locales/ar)

## What is this?

Empty package marker for Arabic locale templates.

## Why does it exist?

Python needs this so `template_parser` can import `stores.llm.templates.locales.ar.rag`.

## Where is it used?

| File | How |
|------|-----|
| `src/stores/llm/templates/locales/ar/rag.py` | Arabic RAG prompts |
