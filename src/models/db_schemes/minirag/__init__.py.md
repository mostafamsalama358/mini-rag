# __init__.py

## What is this?

Empty package marker for `models/db_schemes/minirag/`.

## Why does it exist?

Makes `minirag` a Python sub-package under `db_schemes`.

## Where is it used?

- Python import path: `models.db_schemes.minirag`

## .NET comparison

Empty namespace folder — structure only.
