"""Field pack packages: generic, legal, pharmacy.

Pack configuration is primarily YAML + text. Pharmacy Feature 020 adds optional
recommend helpers under `fields/pharmacy/` (`recommend_pack.py`, `taxonomy_mapper.py`)
as Domain Pack capability loaders — not a new sole owner or Recommendation Service.
"""
